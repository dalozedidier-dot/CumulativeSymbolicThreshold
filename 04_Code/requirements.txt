# Legacy pipeline dependencies — see root pyproject.toml for canonical versions.
# Prefer: pip install -e ".[dev]" from the repository root.

numpy>=1.26
pandas>=2.0
matplotlib>=3.8
scipy>=1.11
statsmodels>=0.14

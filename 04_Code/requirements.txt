# Legacy pipeline dependencies.
# Canonical runtime dependencies live in the root pyproject.toml.
# This file is kept for older scripts and containers that still install with -r.

-r ../requirements.txt

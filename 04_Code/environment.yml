# Legacy pipeline environment — prefer root environment.yml instead.
# Root environment uses: pip install -e ".[dev]" for all dependencies.
name: cumulative_symbolic
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - -e ".[dev]"

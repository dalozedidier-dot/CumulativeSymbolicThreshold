name: QCC StateProb Cross-Conditions

on:
  workflow_dispatch:
    inputs:
      dataset_path:
        description: "Path to dataset (.zip or extracted dir) inside repo"
        required: true
        default: "data/qcc/stateprob/04-09-2020.zip"
        type: string
      auto_plan:
        description: "Auto-select best (algo, device) and compare all available shots"
        required: true
        default: true
        type: boolean
      algo:
        description: "Algo filter (empty = all or autoplan)"
        required: false
        default: ""
        type: string
      device_filter:
        description: "Device filter (empty = all or autoplan)"
        required: false
        default: ""
        type: string
      shots_filter:
        description: "Shots filter (empty = all shots)"
        required: false
        default: ""
        type: string
      metric:
        description: "Ccl metric"
        required: true
        default: "entropy"
        type: choice
        options: ["entropy", "impurity", "one_minus_max"]
      threshold:
        description: "Ccl threshold for t* detection"
        required: true
        default: "0.70"
        type: string
      bootstrap_samples:
        description: "Bootstrap samples"
        required: true
        default: "500"
        type: string

jobs:
  cross:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          if [ -f requirements-qcc-stateprob.txt ]; then
            pip install -r requirements-qcc-stateprob.txt
          elif [ -f requirements-qcc.txt ]; then
            pip install -r requirements-qcc.txt
          else
            pip install numpy pandas matplotlib
          fi

      - name: Run cross-conditions
        shell: bash
        env:
          DATASET_PATH: ${{ inputs.dataset_path }}
          AUTO_PLAN: ${{ inputs.auto_plan }}
          ALGO: ${{ inputs.algo }}
          DEVICE_FILTER: ${{ inputs.device_filter }}
          SHOTS_FILTER: ${{ inputs.shots_filter }}
          METRIC: ${{ inputs.metric }}
          THRESHOLD: ${{ inputs.threshold }}
          BOOTSTRAP_SAMPLES: ${{ inputs.bootstrap_samples }}
        run: |
          set -euo pipefail
          mkdir -p _ci_out/qcc_stateprob_cross

          EXTRA_PLAN=""
          if [ "${AUTO_PLAN}" = "true" ]; then
            EXTRA_PLAN="--auto-plan"
          else
            EXTRA_PLAN="--no-auto-plan"
          fi

          python tools/qcc_stateprob_cross_conditions.py             --dataset-path "${DATASET_PATH}"             --out-root "_ci_out/qcc_stateprob_cross"             --algo "${ALGO}"             --device-filter "${DEVICE_FILTER}"             --shots-filter "${SHOTS_FILTER}"             --metric "${METRIC}"             --threshold "${THRESHOLD}"             --bootstrap-samples "${BOOTSTRAP_SAMPLES}"             ${EXTRA_PLAN}

      - name: Checks
        shell: bash
        run: |
          set -euo pipefail
          python tools/qcc_stateprob_cross_checks.py --out-root "_ci_out/qcc_stateprob_cross"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: qcc_stateprob_cross_artifact
          path: _ci_out/qcc_stateprob_cross

Appliquer ce patch:
1) Copier les fichiers de .github/workflows/ dans ton repo, au même chemin, en remplaçant l'existant.
2) Si tu vois encore un "Invalid workflow file" après ça, c'est qu'il reste un ancien workflow cassé dans .github/workflows. Dans ce cas, supprime ou renomme tout fichier real_data_smoke*.yml en doublon.

# setup.cfg — Minimal residual configuration.
#
# Source of authority: pyproject.toml
# Lint/format: Ruff (configured in pyproject.toml)
# Type checking: mypy (configured in pyproject.toml)
# Testing: pytest (configured in pyproject.toml)
#
# This file is intentionally near-empty.  All lint, format, test
# and type-check settings live in pyproject.toml.  Do NOT add new
# sections here — edit pyproject.toml instead.

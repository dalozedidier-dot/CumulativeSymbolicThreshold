Put your custom real-data CSV files here.
Recommended for long runs: >= 600 rows if you keep the long profile (baseline 50, pre 250, post 250).
The workflow real_data_long_custom.yml expects a file path inside the repo.
